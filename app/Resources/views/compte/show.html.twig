{% extends 'base.html.twig' %}

{% block body %}
    <h1>Compte</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ compte.id }}</td>
            </tr>
            <tr>
                <th>Solde</th>
                <td>{{ compte.solde }}</td>
            </tr>
        </tbody>
    </table>

     <a class="btn btn-link" href="{{ path('cp_index') }}"><i class="fa fa-chevron-left"></i> Back to the list</a>

{% endblock %}
